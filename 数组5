5. 一组无序的自然数集合，由0,1,2... ...,n的数字和一个的数字X（X>=0 && X<=n）组成，请从集合中找出这个重复数字X。
#include<stdio.h>
int main(void)
{
    int i;
    int a[1000]={0};
    while(scanf("%d",&i)!=0)
    {
        a[i]++;
        if(a[i]==2)
        {
            printf("%d",i);
            return 0;
        }
    }
    return 0;
}
答题思路：此题难度一般，就是直接计算每个数字出现的次数，当有2出现的时候就直接输出。
         唯一的难点可能就是题目输入并没有要输入n，每个数字的输入需要思考一下，或者在考有没有更简单的算法解题。
